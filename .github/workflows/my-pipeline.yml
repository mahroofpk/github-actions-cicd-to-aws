name: Pipeline test
on:
  push:
    branches: 
      - master
    

jobs:
  my_testing:
    runs-on: ubuntu-latest
    steps:
      - name: Print nice message from Test
        run: echo "Hello world !!"
  
  my_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Print nice message from Deploy
        run: echo "Hello world from Deploy !!"
      
